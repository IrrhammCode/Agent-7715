{
  "name": "agent-7715-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Agent 7715",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network ethereumSepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network baseSepolia",
    "update-env": "node scripts/update-env.js",
    "verify": "hardhat verify --network ethereumSepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.28.0"
  }
}
